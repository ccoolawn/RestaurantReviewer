
<div class="jumbotron">
	<h2>The Restaurant Reviewer is you premium source for reviews on the worlds best restaurants.</h2>
	<h3>Discover the finest eateries in the world!<br>
	Give your opinions on your favorite places!</h3>
</div>

<table class="table table-bordered table-hover table-condensed">
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @restaurants.each do |restaurant| %>
      <tr>
        <td><%= restaurant.name %></td>
        <td><%= restaurant.address %></td>
        <td><%= restaurant.phone %></td>
        <td><%= link_to restaurant.website, restaurant.website %></td>
        <td><%= link_to 'Show', restaurant, class: "btn btn-default btn-xs" %></td>
        <td><%= link_to 'Edit', edit_restaurant_path(restaurant), class: "btn btn-default btn-xs" %></td>
        <td><%= link_to 'Remove', restaurant, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger btn-xs" %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add a New Restaurant', new_restaurant_path, class: "btn btn-default btn-xs" %>
